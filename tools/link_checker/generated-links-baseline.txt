# Known-broken links in the generated markdown, as of 2026-08-30.
#
# This file is a RATCHET, not a permission slip. The gate blocks any NEW broken
# generated link; these are the ones that predate it, each attributed to the
# ticket that closes it. Delete a line when its ticket lands. Never add a line
# to make a build pass -- a new entry means the gate caught the thing it exists
# to catch.
#
# Baseline history:
#   202  before infra#291 (headings copied into a parent kept the child's base)
#    34  after #291
#    32  after the wrapped-link fix in this PR (both the twin and bundle paths)
#
# One path per line, VARIANT-RELATIVE: the version segment is stripped before
# matching. The same defect appears once per version tree and CI builds ten of
# them (latest, the stable line, three pinned tags, each times two variants), so
# a version-keyed entry would match only the tree it was measured in.
#
# It is also the only form that works for a pinned tag at all: those trees come
# from an immutable tag, so a link broken inside one can never be fixed there.

# ---------------------------------------------------------------------------
# DOC-1506 -- two content pages still document the old `page.md` name, so the
# link resolves to a file the DOC-1432 rename removed. Content fix, not code.
# ---------------------------------------------------------------------------
flyte/api-reference/page.md
union/api-reference/page.md
flyte/community/resources/page.md
union/community/resources/page.md

# ---------------------------------------------------------------------------
# DOC-1511 site A -- `absolutize_links()` resolves an author-written `../x/_index`
# one level too high, dropping a path segment. The gemini and anthropic pages are
# frozen v2.5.8 snapshots of removed plugins and are never regenerated, so these
# two need a content edit rather than a generator fix (see DOC-1512).
# ---------------------------------------------------------------------------
flyte/api-reference/agents/claude
union/api-reference/agents/claude
flyte/api-reference/agents/google
union/api-reference/agents/google
union/union-plugin

# ---------------------------------------------------------------------------
# DOC-1511 -- an `_index` suffix survives resolution instead of being stripped
# to the section landing. Same function, same ticket, distinct symptom.
# ---------------------------------------------------------------------------
union/deployment/byoc/_index
union/deployment/selfmanaged/_index

# ---------------------------------------------------------------------------
# Content, not the generator: author-written ABSOLUTE `/docs/v2/union/...` URLs
# in prose, pointing at paths that no longer exist. The generator never touches
# an already-absolute link, so no code change reaches these. Each needs the
# author's intended target restored.
#   considerations/            -> page removed or renamed
#   task-programming/traces/   -> now under user-guide/tasks/task-programming/
#   reusable-containers        -> now under user-guide/tasks/task-configuration/
#   sandboxing/code-sandboxing -> now under user-guide/agents/sandboxing/
# ---------------------------------------------------------------------------
union/user-guide/considerations/#driver-pod-requirements
union/user-guide/task-programming/traces/
union/user-guide/reusable-containers
union/user-guide/sandboxing/code-sandboxing
