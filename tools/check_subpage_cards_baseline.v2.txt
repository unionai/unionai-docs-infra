# Section pages with children that have no subpage cards, as of 2026-08-30.
#
# A RATCHET, not a permission slip. The gate's job is to stop a NEW section page
# shipping with no way forward for a reader; these predate it. DOC-1509 is
# explicit that they must not be auto-carded -- deciding whether a landing page
# wants cards, and what belongs above them, is an editorial call per page.
#
# Delete a line when the page gets `{{< subpage-cards >}}`.
#
# A page that should never have cards -- a document rather than a signpost --
# stays here permanently, with the reason in a comment above it. That is the one
# entry kind that does not shrink, and it is why this file also replaced the
# `subpage_cards: false` front-matter key: one place to look, not two.
# Never add a line to make a build pass: a new entry means the gate caught the
# thing it exists to catch.
#
# 46 entries. By area: deployment 18, user-guide 13, security 5, integrations 4,
# oss-deployment 3, community 2, tutorials 1, plus the site root.

# The site root is a redirect to user-guide. No reader ever sees it, so it will
# never want cards. This entry is permanent.
_index.md
